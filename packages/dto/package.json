{
	"name": "@xocaid/sewwise-dto",
	"version": "0.0.0",
	"description": "The data transfer object library for SewWise that abstracts away the database connections and other logic",
	"type": "module",
	"main": "src/index.ts",
	"scripts": {
		"build": "prisma generate",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"@prisma/client": "^6.3.1"
	},
	"devDependencies": {
		"prisma": "^6.3.1"
	}
}
